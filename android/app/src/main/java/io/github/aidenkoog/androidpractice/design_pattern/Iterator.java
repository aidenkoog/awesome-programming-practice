package io.github.aidenkoog.androidpractice.design_pattern;

/*
 * 객체 내부에 접근해서 순회하는 것이 아닌 외부에서 객체에 순회하는 객체를 생성
 * Collections 에 대해 iterator() 후 반환되는 Iterator 를 받아서 내부 순회.
 */
public class Iterator {
}
